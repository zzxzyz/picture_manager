你是一个专业的python工程师，比较A和B两个文件夹下面的文件是否全部相同，如果两个文件的md5值一样，则认为文件是一样的，否则认为文件不一样
按如下步骤实现：
1. 分别遍历A和B两个文件夹，得到A的文件列表和B的文件列表
2. 遍历A和B文件列表，计算每个文件的md5值，得到A的md5列表和B的md5列表
3. 计算A的md5列表和Bmd5列表的交集
4. 计算在A中但不在B中的文件列表
5. 计算在B中但不在A中的文件列表

如果A和B两个文件夹的文件完全一样，则输出yes, 否则输出A的差异文件列表和B的差异文件列表
用python实现之后，给出测试用例，确保功能完全符合预期


#1
你是一个专业的python工程师, 遍历文件夹，文件夹下面没有其它子文件夹，找出所有内容相同的文件，按分组输出重复文件的列表，然后只保留其中一个副本即可，其它全部删除，
功能实现之后，给出测试用例，确保功能完全符合预期

#2 
你是一个专业的python工程师，有两个文件夹，文件夹下面没有其它子文件夹，文件夹下面所有文件的内容都是各不相同的，你需要对比两个文件夹，
找出里面内容相同的文件，
例子1：目录A和目标B，目录A下面有文件[1.txt, 2.txt, 3.txt], 目录B下面有[a.txt, b.txt, c.txt];
其中1.txt和c.txt, 2.txt和a.txt, 3.txt 和b.txt 内容完全相同; 则认为两个文件夹是一模一样的，结果输出全部一样。
例子2：目录A和目标B，目录A下面有[1.txt, 2.txt, 3.txt], 目录B下面有[a.txt, b.txt];
其中2.txt和a.txt, 3.txt 和b.txt 内容完全相同，则认为两个文件夹有差异，A存在B没有的文件1.txt, 
例子3：目录A和目标B，目录A下面有[1.txt, 2.txt], 目录B下面有[a.txt, b.txt, c.txt];
其中1.txt和c.txt, 2.txt和a.txt, 内容完全相同; 则认为两个文件夹有差异，B存在A没有的文件b.txt, 
例子4：目录A和目标B，目录A下面有[1.txt, 2.txt, 3.txt], 目录B下面有[a.txt, b.txt, c.txt]; 
其中1.txt和c.txt, 2.txt和a.txt, 内容完全相同; 则认为两个文件夹有差异，B存在A没有的文件b.txt, A存在B没有的文件3.txt